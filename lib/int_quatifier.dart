extension IntQuatifier on int {
  Duration day() => Duration(days: this);
  Duration days() => Duration(days: this);
  Duration hour() => Duration(hours: this);
  Duration hours() => Duration(hours: this);
  Duration minute() => Duration(minutes: this);
  Duration minutes() => Duration(minutes: this);
  Duration second() => Duration(seconds: this);
  Duration seconds() => Duration(seconds: this);
  Duration microsecond() => Duration(microseconds: this);
  Duration microseconds() => Duration(microseconds: this);
  Duration millisecond() => Duration(milliseconds: this);
  Duration milliseconds() => Duration(milliseconds: this);
}
